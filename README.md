# Setup for Ubuntu 26
```bash
#### Install programs
bash install/0_pm.bash
bash install/1_non-gui.bash
bash install/2_docker.bash
bash install/3_gui.bash

#### Configure settings
GIT_NAME=""
GIT_EMAIL=""
bash settings.bash ${GIT_NAME} ${GIT_EMAIL}
```

